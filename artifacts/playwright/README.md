# Evidencia Playwright

Este directorio recibe capturas saneadas de escenarios exitosos contra FastAPI
y SQLite efímeros. No debe contener credenciales, tokens ni datos reales.

`bitacora-real-completa.png` se regenera con `npm run test:e2e:real`.
